interface ValueCard {
  image: Image;
  title: string;
  description: string;
}

interface BenefitCard {
  image: Image;
  title: string;
  description: string;
}

interface CareCard {
  title: string;
  image: Image;
}
